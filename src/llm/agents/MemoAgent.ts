import Agent from "../agent.js";

const personalAgent = new Agent(
	"MEMO",
	`
Call it for:
- Store files, texts or websites.
- You want to retrieve information previously stored through a word or phrase.
Generally the information is relevant to the user's context.
For example: ...
`,
	[],
	{}
)